[project]
name = "case-study"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "apache-airflow==2.10.0",
    "dbt-duckdb>=1.10.1",
    "pandas>=3.0.2",
    "requests>=2.33.1",
]
